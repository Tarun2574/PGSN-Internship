# Commands Used – Day 2 Terraform Modules, Remote Backend & Workspaces

```bash
# Re-initialize after adding a module and/or backend configuration
terraform init
```

> Note: only the command visible in the captured screenshots is listed here. Workspace commands (`terraform workspace ...`) were part of the day's learning objectives but are not included since no corresponding screenshot evidence was captured.
